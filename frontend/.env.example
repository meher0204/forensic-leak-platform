# API base URL — used by all frontend API calls and download links.
# In development, the Vite proxy (vite.config.ts) forwards /api/*
# to the backend, so leave this commented out for same-origin requests.
# For production, uncomment and point to your deployed API server.
VITE_API_BASE_URL=http://localhost:8000
